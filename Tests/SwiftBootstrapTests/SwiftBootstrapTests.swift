import XCTest
@testable import SwiftBootstrap

final class SwiftBootstrapTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftBootstrap().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
